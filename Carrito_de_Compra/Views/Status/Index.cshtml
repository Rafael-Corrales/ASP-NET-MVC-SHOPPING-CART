@model IEnumerable<Carrito_de_Compra.Models.Status>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreStatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdStatus }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdStatus }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdStatus })
        </td>
    </tr>
}

</table>
