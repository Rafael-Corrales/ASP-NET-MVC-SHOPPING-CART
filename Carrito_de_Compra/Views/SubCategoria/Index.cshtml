@model IEnumerable<Carrito_de_Compra.Models.SubCategoria>

@{
    ViewBag.Title = "Index";
}

<h2 class="text-center">Subcategorias</h2>

<p class="text-center">
    @Html.ActionLink("Crear Nueva", "Create","Subcategoria", null, new { @class = "btn btn-primary btn-large" })
</p>
<div class="container">
    <table id="datatable" class="table table-striped table-bordered dt-responsive nowrap" width="100%" cellspacing="0">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.NombreSubCategoria)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Categoria.NombreCategoria)
                </th>
                <th>            @Html.DisplayNameFor(model => model.Activo)</th>
                <th> Acciones
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.NombreSubCategoria)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Categoria.NombreCategoria)
                    </td>
                    <td>
                        <input type="checkbox" id="IsActive_@item.IdSubCategoria" @(item.Activo ? "checked" : string.Empty) class="filled-in" disabled />
                        <label for="IsActive_@item.Activo"></label>
                    </td>
                    <td>
                        @Html.ActionLink("Editar", "Edit","Subcategoria", new { id = item.IdSubCategoria }, new { @class = "btn btn-primary btn-large" })
                        @Html.ActionLink("Detalles", "Detail", "Subcategoria", new { id = item.IdSubCategoria }, new { @class = "btn btn-primary btn-large" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<div>
    @Html.ActionLink("Back to Dashboard", "Dashboard", "Home")
</div>
    <script>


        $('#datatable').dataTable({
            "language": {
                "lengthMenu": "Mostrar _MENU_ registros por página",
                "zeroRecords": "No hay registros para mostrar",
                "info": "Mostrando  _PAGE_ de _PAGES_ registros",
                "infoEmpty": "No hay registros disponibles",
                "infoFiltered": "(Filtrando un total de _MAX_  registros)",
                "loadingRecords": "Cargando...",
                "processing": "Procesando...",
                "search": "Busqueda:",
                "zeroRecords": "No hay registros",
                "paginate": {
                    "first": "Primero",
                    "last": "Ultimo",
                    "next": "Siguiente",
                    "previous": "Anterior"
                }
            },
        });
    </script>
